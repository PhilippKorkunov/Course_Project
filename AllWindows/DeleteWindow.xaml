<Window x:Class="Course_Project.AllWindows.DeleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Course_Project.AllWindows"
        mc:Ignorable="d"
        Title="Удалить Запись" Height="200" Width="400">

    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFEBDADA" Offset="0.9"/>
        </LinearGradientBrush>
    </Window.Background>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="Header" Text="Укажите ID запесей для удаления через запятую" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"
                    Grid.Row="0"/>

        <TextBox x:Name="IdForDeleteBox" Grid.Row="1" VerticalAlignment="Center" Height="25" Width="325" FontSize="12" Padding="3"/>

        <Button x:Name="DeleteButton" Content="Удалить" Grid.Row="2" VerticalAlignment="Top" Height="30" Width="150" Background="Cornsilk"/>
    </Grid>
</Window>
